{
  "name": "brp-extension",
  "version": "0.3.0",
  "private": true,
  "description": "BRP Firefox Extension - test infrastructure",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
