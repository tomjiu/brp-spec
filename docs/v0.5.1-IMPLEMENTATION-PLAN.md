# v0.5.1 Implementation Plan

> User Trust — Visibility + Control

**Status**: Draft
**Prerequisite**: v0.5.0 released (tag `v0.5.0`)

---

## 1. v0.5.1 Goal

v0.5.0 solved "safety" (E1/E2/E5/B2). v0.5.1 solves "trust":
- User can see BRP connection state at a glance (extension icon)
- User can see which tab AI is operating (page indicator)
- User can explicitly trust domains (allowlist)

### Non-goals (deferred)

| Item | Deferred to | Reason |
|------|:---:|--------|
| tabGroups coloring | v0.5.2 | Zen compatibility unknown, needs real-machine test |
| Tab-level permissions | v0.5.2 | Larger scope, separate version |
| Browser history access | v0.5.2 | Optional permission, AMO review risk |

---

## 2. Sub-PR Breakdown

### PR #44: Status Icon (2-3 days)

Extension icon shows 4 states + badge with controllable tab count.

### PR #45: Page Indicator (2-3 days)

Content script injects floating status bar on each page.

### PR #46: Domain Allowlist (2-3 days)

User-configurable allowlist, skips E1 ask for trusted domains.

### PR #47: v0.5.1 Release (0.5 day)

Version bump + CHANGELOG + tag.

---

## 3. After v0.5.1

### v0.5.2 — Tab-level Permissions + tabGroups

- Protected tabs (user-controlled controllable state)
- tabGroups coloring (Firefox v139+, Zen real-machine verify)
- Browser history access (optional permission)

### v0.6.0 — Tech Debt

- ts-rs type alignment
- Rust struct real usage

### v1.0-pre — Stabilization

- Integration tests
- Multi-browser compatibility matrix
- API freeze
